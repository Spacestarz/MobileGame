using UnityEngine;

public class CardObject : MonoBehaviour
{
    public Card _card = null;
    public CardVisuals _visuals = null;

    void Start()
    {
        // start _visuals.Init(_card);
    }

    void Update()
    {
        // card behaviour
        // drag, drop etc
    }
}
